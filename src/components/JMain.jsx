import React from 'react';
import JLocationComp from './JLocationComp';

const JMain = ({data}) => {

    return (
        <main>
            <JLocationComp data={data} />
        </main>
    );
};

export default JMain;